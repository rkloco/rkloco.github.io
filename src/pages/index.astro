---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Ronit Kapur">
	<div class="log-page">
		<div class="sky" id="sky" aria-hidden="true"></div>
		<div class="log-inner">
			<header class="log-header">
				<div class="header-top">
					<div class="meta-stack">
						<h1>Ronit Kapur</h1>
						<p class="role">duke // cs + physics</p>
						<p class="loc">durham, nc</p>
					</div>
					<canvas class="oscilloscope" id="oscilloscope" aria-hidden="true"></canvas>
				</div>

				<div class="links">
					<a href="https://linkedin.com/in/ronit-kapur" target="_blank">linkedin</a>
					<span class="sep">·</span>
					<a href="mailto:ronit.kapur@duke.edu">email</a>
					<span class="sep">·</span>
					<a href="https://github.com/rkloco" target="_blank">github</a>
					<span class="sep">·</span>
					<a href="/gallery">gallery</a>
					<span class="sep">·</span>
					<a href="/blog">writing</a>
					<span class="sep">·</span>
					<a href="/misc">misc</a>
				</div>

				<p class="blurb">
					Rising third-year at Duke University studying CS and Physics — concentrations in AI/ML and astrophysics along with a math minor. 
					I'm interested in developing technology that creates or uses fresh, insightful models of the world in its manifestation.
					I'm interested in profilerating this technology thoughtfully to make the world a better place.
					Previously at TJHSST in Alexandria, VA. Love meeting new people — always feel free to reach out. Views are my own. 
				</p>
			</header>

			<section>
				<div class="sec-head">
					<span class="hash">#</span>
					<span class="name">current_ops</span>
					<span class="meta">as of 2026-05</span>
				</div>
				<hr />

				<div class="entry">
					<span class="tag">T-3 mo</span>
					<p>Studying abroad at the <strong>University of Edinburgh</strong> for the fall semester.</p>
				</div>

				<div class="entry">
					<span class="tag">T+0 mo</span>
					<p>GNC engineering at <strong>SpaceX</strong> in Hawthorne, CA on the Starshield program. Designing and implementing guidance, navigation, and control algorithms, simulations, tools, and ops dashboards in support of national security.</p>
				</div>

				<div class="entry">
					<span class="tag">T+12 mo</span>
					<p>Lead software engineer at <strong>Liberata</strong>. Building an open-source academic publishing platform with peer-review and replication incentives — data infrastructure, file processing, integration of novel scholarly metrics.</p>
				</div>
			</section>

			<section>
				<div class="sec-head">
					<span class="hash">#</span>
					<span class="name">research_log</span>
				</div>
				<hr />

				<div class="entry">
					<span class="tag">2022–24</span>
					<p>Independent astrophysics research at <strong>UMD</strong> in collaboration with NASA, advised by Tad Komacek. Incorporated stellar gravity darkening into Navier-Stokes general circulation models — the most sophisticated atmospheric model of exoplanets at time of completion. Months-long simulations on UMD's Zaratan cluster. Presented at <strong>ISEF 2023</strong> in Dallas.</p>
				</div>

				<div class="entry">
					<span class="tag">2022–23</span>
					<p>Research at the <strong>U.S. Naval Research Laboratory</strong>. Modeled the solar magnetic field for space weather prediction and mitigation. Examined Faraday Rotation near the Sun using Parker Solar Probe ephemeris to identify optimal observation windows in support of a successful Very Large Array proposal. Presented at <strong>ISDC 2025</strong> in Washington, D.C.</p>
				</div>
			</section>

			<section>
				<div class="sec-head">
					<span class="hash">#</span>
					<span class="name">industry_log</span>
				</div>
				<hr />

				<div class="entry">
					<span class="tag">summer 2025</span>
					<p>Policy research at the <strong>GWU Space Policy Institute</strong>. Wrote a technical report on space governance grounded in Svalbard and Tangier precedent.</p>
				</div>

				<div class="entry">
					<span class="tag">spring 2025</span>
					<p>Data engineer at <strong>Turbo AI</strong>. Helped scale an edtech AI startup from 1.5M to 3M users and past $3M ARR. PostHog instrumentation, retention and churn modeling, pricing and UI iteration.</p>
				</div>
			</section>

			<section>
				<div class="sec-head">
					<span class="hash">#</span>
					<span class="name">teaching</span>
				</div>
				<hr />

				<div class="entry">
					<span class="tag">spring 2026</span>
					<p>Help Room TA for <strong>Math 230 — Probability</strong>.</p>
				</div>

				<div class="entry">
					<span class="tag">fall 2025</span>
					<p>Help Room TA for <strong>Math 218D — Matrices &amp; Vectors</strong>, Duke's linear algebra course for engineers.</p>
				</div>
			</section>

			<section>
				<div class="sec-head">
					<span class="hash">#</span>
					<span class="name">recognition</span>
				</div>
				<hr />
				<p class="oneline">8VC × Meta × Duke Hackathon, 1st · ISEF Finalist · SciOly Astronomy 1st nationwide · National Merit Scholar · WTOP "Top Kid".</p>
			</section>

			<p class="footer-line">Site built with <strong>Astro</strong>.</p>
		</div>
	</div>
</Layout>

<style>
	.log-page {
		position: relative;
		min-height: 100vh;
		padding: 64px 24px 96px;
	}

	.sky {
		position: fixed;
		inset: 0;
		z-index: 1;
		pointer-events: none;
		overflow: hidden;
	}

	.log-inner {
		position: relative;
		z-index: 2;
		max-width: 720px;
		margin: 0 auto;
	}

	.log-header {
		margin-bottom: 56px;
	}

	.header-top {
		display: flex;
		gap: 24px;
		align-items: flex-start;
		margin-bottom: 18px;
	}

	.meta-stack {
		flex: 1;
		min-width: 0;
	}

	.oscilloscope {
		display: block;
		width: 110px;
		height: 110px;
		flex-shrink: 0;
	}

	.log-header h1 {
		font-size: 28px;
		font-weight: 700;
		color: var(--text-strong);
		letter-spacing: -0.01em;
		margin: 0 0 4px;
	}

	.role { color: var(--text-dim); margin: 0; }
	.meta-stack .loc { color: var(--text-dimmer); margin: 0; }
	.loc { color: var(--text-dimmer); margin: 0 0 18px; }

	.links {
		margin-bottom: 28px;
		color: var(--text-dimmer);
	}

	.links a {
		color: var(--text);
		text-decoration: none;
		border-bottom: 1px dotted var(--dot);
		padding-bottom: 1px;
		transition: border-color 0.15s, color 0.15s;
	}

	.links a:hover {
		color: var(--text-strong);
		border-bottom-color: var(--text);
	}

	.sep { color: var(--text-faint); margin: 0 8px; }

	.blurb { color: var(--text); margin: 0; }

	section { margin-bottom: 40px; }

	.entry {
		display: grid;
		grid-template-columns: 100px 1fr;
		gap: 16px;
		margin-bottom: 18px;
	}

	.tag {
		color: var(--text-dimmer);
		font-size: 12px;
		padding-top: 2px;
		white-space: nowrap;
	}

	.entry p { margin: 0; color: var(--text); }

	.entry strong {
		color: var(--text-strong);
		font-weight: 500;
		border-bottom: 1px dotted var(--dot);
		padding-bottom: 1px;
	}

	.oneline { color: var(--text); margin: 0; }

	.oneline a {
		color: var(--text-strong);
		text-decoration: none;
		border-bottom: 1px dotted var(--dot);
		padding-bottom: 1px;
	}
	.oneline a:hover { border-bottom-color: var(--text); }

	.footer-line {
		margin-top: 64px;
		text-align: center;
		color: var(--text-dimmer);
		font-size: 12px;
	}

	.footer-line strong {
		color: var(--text);
		font-weight: 500;
	}

	@media (max-width: 640px) {
		.entry {
			grid-template-columns: 1fr;
			gap: 4px;
		}
		.tag { padding-top: 0; }
	}
</style>

<script>
	function initShootingStars() {
		const sky = document.getElementById('sky');
		if (!sky) return;

		const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
		if (reduce) return;

		function spawn() {
			const star = document.createElement('div');
			star.className = 'shooting-star';

			const W = window.innerWidth;
			const H = window.innerHeight;

			const startX = W * 0.2 + Math.random() * (W * 0.95);
			const startY = -40 - Math.random() * 80;

			const angleDeg = 110 + Math.random() * 50;
			const angleRad = angleDeg * Math.PI / 180;

			const dist = Math.hypot(W, H) * (0.9 + Math.random() * 0.4);
			const dx = Math.cos(angleRad) * dist;
			const dy = Math.sin(angleRad) * dist;

			const dur = 1100 + Math.random() * 900;

			star.style.left = startX + 'px';
			star.style.top = startY + 'px';
			star.style.setProperty('--dx', dx + 'px');
			star.style.setProperty('--dy', dy + 'px');
			star.style.setProperty('--angle', angleDeg + 'deg');
			star.style.animation = `shoot ${dur}ms cubic-bezier(0.4, 0.05, 0.6, 0.95) forwards`;
			const w = 80 + Math.random() * 70;
			star.style.width = w + 'px';
			star.style.opacity = String(0.7 + Math.random() * 0.3);

			sky.appendChild(star);
			setTimeout(() => star.remove(), dur + 100);
		}

		function loop() {
			spawn();
			if (Math.random() < 0.18) {
				setTimeout(spawn, 200 + Math.random() * 400);
			}
			const next = 3500 + Math.random() * 5000;
			setTimeout(loop, next);
		}

		let started = false;
		function start() {
			if (started) return;
			started = true;
			setTimeout(loop, 600);
		}
		if (document.visibilityState === 'visible') start();
		document.addEventListener('visibilitychange', () => {
			if (document.visibilityState === 'visible') start();
		});
	}

	function initOscilloscope() {
		const canvas = document.getElementById('oscilloscope');
		if (!canvas) return;
		const ctx = canvas.getContext('2d');
		if (!ctx) return;

		const dpr = window.devicePixelRatio || 1;
		const CSS_SIZE = 110;
		canvas.width = CSS_SIZE * dpr;
		canvas.height = CSS_SIZE * dpr;
		canvas.style.width = CSS_SIZE + 'px';
		canvas.style.height = CSS_SIZE + 'px';
		ctx.scale(dpr, dpr);

		const W = CSS_SIZE;
		const H = CSS_SIZE;
		const cx = W / 2;
		const cy = H / 2;
		const R = 34;            // amplitude
		const X_FREQ = 3;
		const Y_FREQ = 4;
		const N_ECHOES = 20;     // brightness segments along the curve
		const N_STEPS = 6;       // points per echo (smoothness)
		const SPEED = 0.9;       // full cycles per second

		// Theme-reactive line color (canvas itself is transparent — page bg shows through)
		let lineRGB;
		function refreshColors() {
			const light = document.documentElement.getAttribute('data-theme') === 'light';
			lineRGB = light ? [42, 42, 42] : [245, 245, 245];
		}
		refreshColors();

		function curvePoint(phase) {
			const angle = 2 * Math.PI * phase;
			return [
				cx + R * Math.cos(X_FREQ * angle),
				cy + R * Math.sin(Y_FREQ * angle)
			];
		}

		function drawFrame(headPhase) {
			ctx.clearRect(0, 0, W, H);

			const echoSpan = 1 / N_ECHOES;
			ctx.lineWidth = 1.3;
			ctx.lineCap = 'round';
			ctx.lineJoin = 'round';

			// e = 0 is oldest/dimmest, e = N_ECHOES-1 is freshest (brightest, just behind head)
			for (let e = 0; e < N_ECHOES; e++) {
				const startPhase = headPhase - (N_ECHOES - e) * echoSpan;
				const alpha = (e + 1) / N_ECHOES;
				ctx.strokeStyle = `rgba(${lineRGB[0]}, ${lineRGB[1]}, ${lineRGB[2]}, ${alpha})`;
				ctx.beginPath();
				for (let i = 0; i <= N_STEPS; i++) {
					const p = startPhase + (i / N_STEPS) * echoSpan;
					const [x, y] = curvePoint(p);
					if (i === 0) ctx.moveTo(x, y);
					else ctx.lineTo(x, y);
				}
				ctx.stroke();
			}
		}

		// Reduced motion → static full curve
		const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
		function drawStatic() {
			ctx.clearRect(0, 0, W, H);
			ctx.strokeStyle = `rgba(${lineRGB[0]}, ${lineRGB[1]}, ${lineRGB[2]}, 0.9)`;
			ctx.lineWidth = 1.3;
			ctx.beginPath();
			for (let i = 0; i <= 400; i++) {
				const [x, y] = curvePoint(i / 400);
				if (i === 0) ctx.moveTo(x, y);
				else ctx.lineTo(x, y);
			}
			ctx.stroke();
		}

		// Redraw immediately on theme change (covers both animated and static modes)
		new MutationObserver(() => {
			refreshColors();
			if (reduce) drawStatic();
		}).observe(document.documentElement, {
			attributes: true,
			attributeFilter: ['data-theme'],
		});

		if (reduce) {
			drawStatic();
			return;
		}

		let headPhase = 0;
		let last = performance.now();
		let raf = 0;

		function tick(now) {
			const dt = Math.min(0.05, (now - last) / 1000);
			last = now;
			headPhase = (headPhase + SPEED * dt) % 1;
			drawFrame(headPhase);
			raf = requestAnimationFrame(tick);
		}

		raf = requestAnimationFrame(tick);
		document.addEventListener('visibilitychange', () => {
			if (document.visibilityState === 'visible') {
				last = performance.now();
				raf = requestAnimationFrame(tick);
			} else {
				cancelAnimationFrame(raf);
			}
		});
	}

	function init() {
		initShootingStars();
		initOscilloscope();
	}

	if (document.readyState === 'loading') {
		document.addEventListener('DOMContentLoaded', init);
	} else {
		init();
	}
</script>
